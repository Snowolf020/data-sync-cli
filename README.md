# Data Sync CLI
A command-line tool for synchronizing data between PostgreSQL and SQLite databases using Prisma and Redis for caching.
## Installation
```bash
npm install -g data-sync-cli
```
## Usage
```bash
data-sync-cli --help
```
## Contributing
Contributions are welcome.